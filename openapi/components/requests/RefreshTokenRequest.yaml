type: object
description: >-
  Shared by POST /auth/refresh and POST /auth/logout, both of which act on
  a single refresh token.
required:
  - refreshToken
properties:
  refreshToken:
    type: string
    minLength: 1
