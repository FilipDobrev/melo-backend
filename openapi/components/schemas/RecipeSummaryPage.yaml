description: A page of recipe summaries.
allOf:
  - $ref: './PaginationFields.yaml'
  - type: object
    required: [items]
    properties:
      items:
        type: array
        items:
          $ref: './RecipeSummary.yaml'
