description: The requested resource does not exist.
content:
  application/json:
    schema:
      $ref: '../schemas/Error.yaml'
    example:
      error:
        code: NOT_FOUND
        message: Resource not found
